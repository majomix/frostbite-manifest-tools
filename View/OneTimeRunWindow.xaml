<Window
    x:Class="FrostbiteManifestSystemTools.View.OneTimeRunWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Frostbite 3 Manifest Editor"
    Width="450"
    Height="250"
    ResizeMode="NoResize">
    <Window.Resources>
        <BooleanToVisibilityConverter
            x:Key="booleanToVisibilityConverter" />
    </Window.Resources>
    <DockPanel>
        <TextBlock
            HorizontalAlignment="Center"
            DockPanel.Dock="Bottom"
            Text="Processing game files. Please wait..." />
        <TextBlock
            x:Name="ErrorText"
            HorizontalAlignment="Center"
            DockPanel.Dock="Bottom"
            Text="Error occured."
            Visibility="{Binding Path=HasError, Converter={StaticResource booleanToVisibilityConverter}}" />
        <Grid
            Height="50"
            DockPanel.Dock="Bottom">
            <ProgressBar
                Value="{Binding CurrentProgress, Mode=OneWay}" />
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Text="{Binding CurrentProgress, StringFormat={}{0} %}" />
        </Grid>

        <Grid
            x:Name="imageGrid">
            <Grid.Background>
                <ImageBrush>
                    <ImageBrush.ImageSource>
                        <BitmapImage
                            UriSource="Frostbite.bmp" />
                    </ImageBrush.ImageSource>
                </ImageBrush>
            </Grid.Background>
        </Grid>
    </DockPanel>
</Window>